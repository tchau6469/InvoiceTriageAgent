# Project scripts

Command-line entry points will live here. They should remain thin wrappers over
importable application services under `src/invoice_triage/`.

Planned commands include corpus ingestion and retrieval evaluation.
